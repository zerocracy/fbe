(award
  (explain "When someone pushes directly to the default branch, they get a penalty")
  (aka
    (let fee {{ -8 | times: anger }})
    (let penalty {{ 8 | times: anger }})
    (give fee "as a basis")
    "deduct ${penalty} points")
  )
